package com.example.covid19ciudados.models

class SliderItem(image: Int, desc: String) {
    var image: Int = 0
    var desc: String = ""

    init {
        this.image = image
        this.desc = desc
    }
}